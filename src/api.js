const Api = {
  getMovies: () => {
    return `getMovies() response: ${new Date()}`;
  },
  getSingleMovie: () => {
    return `getSingleMovie() response: ${new Date()}`;
  },
};

export default Api;
